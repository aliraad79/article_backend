version: '3.9'

services:
  # web:
  #   build: articles
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - db

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
  db:
    image: "postgres:15.4"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=articles
    ports:
      - "5432:5432"